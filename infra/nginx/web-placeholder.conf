server {
    listen 80;
    server_name _;

    location = /healthz {
        access_log off;
        add_header Content-Type text/plain;
        return 200 "ok\n";
    }

    location / {
        add_header Content-Type application/json;
        return 503 '{"status":"not_implemented","component":"web"}';
    }
}
